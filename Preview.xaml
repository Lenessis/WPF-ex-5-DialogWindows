<Window x:Class="zad5.Preview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:zad5"
        mc:Ignorable="d"
        Title="Preview" Height="450" Width="400"
        Loaded="Window_Loaded" Closed="ClosePreview">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <Label Name="MoviePreview" Grid.Row="0" Padding="5" Margin="10"/>
       <!-- <WrapPanel Grid.Row="1" HorizontalAlignment="Center">
            <Button Click="Ok" IsDefault="True" Padding="30,10" Margin="10" >OK</Button>
        </WrapPanel> -->
        
    </Grid>
</Window>
